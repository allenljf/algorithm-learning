import 'package:flutter_test/flutter_test.dart'; import 'package:learning_app/features/library/contracts.dart'; void main(){test('repository delegates paginated list contract',() async {final page=await RemoteProblemRepository(_Fake()).list(query:'sum');expect(page.items.single.title,'Two Sum');});}class _Fake implements ProblemRemote {@override Future<Page<ProblemSummary>> listProblems({String? query,int page=1})async=>const Page(items:[ProblemSummary(id:'p',title:'Two Sum',difficulty:'easy',tags:[])],page:1,pageSize:20,totalItems:1);}
